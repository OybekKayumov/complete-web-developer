import React from 'react'

const Scroll = (props) => {
  console.log(props );
  return (
    // <div>
      props.children
    // </div>
  )
}

export default Scroll

// props
// state
// children (components)
